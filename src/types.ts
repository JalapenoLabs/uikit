// Copyright © 2026 Jalapeno Labs

// Re-exported from type-fest, which is a dev-only dependency. The dts build
// inlines these into the published declarations (see `bundledPackages` in
// vite.config.ts), so consumers never need type-fest installed.
export type { Promisable, LiteralUnion } from 'type-fest'
